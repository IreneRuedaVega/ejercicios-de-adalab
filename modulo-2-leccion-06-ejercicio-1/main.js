"use strict";

const adalaber1 = {
  name: "Susana",
  age: 34,
  profession: "periodista",
};

const adalaber2 = {
  name: "Rocío",
  age: 25,
  profession: "actriz",
};

console.log(
  `Mi nombre es ${adalaber1.name}, tengo ${adalaber1.age} y soy ${adalaber1.profession}.`
);

console.log(
  `${adalaber2.name} es una chica que tiene ${adalaber2.age} años y trabaja como ${adalaber2.profession}.`
);
